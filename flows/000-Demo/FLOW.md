---
name: Demo
description: EasyCC Demo Flow — 展示目录约定
version: "1.0.0"
tools: read, write
---

# Demo Flow

这是一个 Demo Flow，用于验证框架的 Flow 发现和管理功能。

## 使用方法

1. 在 `input/` 放入文件
2. 通过聊天发送指令
3. 结果输出到 `output/`
